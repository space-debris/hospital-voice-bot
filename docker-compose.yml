services:
  app:
    build: .
    container_name: hospital-voice-bot
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
